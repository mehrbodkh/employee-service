ktor:
  environment: "DEV" # DEV, PROD
  application:
    modules:
        - com.mehrbod.util.ApplicationKt.module
  deployment:
    port: 8080
database:
  h2:
    host: "localhost"
    driver: "h2"
    protocol: "mem"
    database: "test"
    user: ""
    password: ""
  postgres:
    url: "postgresql://localhost/default"
    host: "localhost"
    driver: "postgres"
    protocol: "file"
    database: "employee"
    user: "username"
    password: "password"
kafka:
  schema:
    registry:
      url: "http://schema-registry:8081"
  common:
    bootstrap:
      servers: "broker:9092"
  admin:
  consumer:
    group.id: "notification-group"
  producer:
    client.id: "my-client-id"
